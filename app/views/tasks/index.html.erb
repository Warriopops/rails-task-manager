<h1>test</h1>
<%= link_to "Nouvelle tache",  tasks_new_path %>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %></li>
    <%= link_to "Details", task_path(task.id) %>
    <%= link_to "Edit", edit_path(task.id) %>
    <%= link_to "Delete",
  task_path(task.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
  <% end %>
</ul>
